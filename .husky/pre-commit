# Run lint-staged to auto-fix and format staged files
npx lint-staged

# Run full lint check (will fail if there are unfixable errors)
npm run lint

# Run build check (will fail if TypeScript compilation fails)
npm run build

